#include <stdio.h>

/* Invierte datos
El programa,al recibir como dato un conjunto de detos de entrada, invierte
orden de los mismos cuando los imprime.

A, B. C. D: variables de tipo entero. */

void main(void)
{
int A, B, C, D;
printf("Ingrese cuatro datos de tipo entero: ");
scanf("%d %d %d", D, C, B, A);
}
